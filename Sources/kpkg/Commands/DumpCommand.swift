//
//  extract.swift
//  
//
//  Created by Christophe Bronner on 2024-06-22.
//

import ArgumentParser

struct DumpCommand: ParsableCommand {

	static let configuration = CommandConfiguration(
		commandName: "dump",
		abstract: "Prints information about the given package or stub.")

	func run() throws {
//		inputPackage = fopen(packager.filename, "rb");
//		if (!inputPackage) {
//			printf("Couldn't find %s. Aborting operation.\n", packager.filename);
//		} else {
//			printMetadata(inputPackage);
//			fclose(inputPackage);
//			printf("\n");
//		}
	}

}
